[build]
pkgManager = "pip"

[build.apt]
packages = [
  "libglib2.0-0",
  "libsm6",
  "libxrender1",
  "libxext6",
  "libgl1",
  "libgl1-mesa-glx",
  "libegl1",
  "libgles2-mesa"
]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
