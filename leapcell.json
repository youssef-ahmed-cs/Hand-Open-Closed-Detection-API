{
  "build": {
    "builder": "python"
  },
  "runtime": {
    "nodeVersion": "18"
  },
  "deployment": {
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
